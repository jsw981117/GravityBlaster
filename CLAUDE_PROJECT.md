# GravityBlaster (Gravity Line) 개발 가이드라인

## 프로젝트 개요
- **목적**: 중력 방향을 변경하며 블록을 매칭하는 웹 기반 퍼즐 게임
- **기술 스택**: Vanilla JavaScript, HTML5 Canvas, CSS3
- **주요 기능**:
  - 8x8 그리드 기반 블록 매칭 게임
  - 중력 방향 변경 (상하좌우)
  - 블록 애니메이션 (이동, 생성, 제거)
  - 폭탄 블록 시스템
  - 점수 시스템 및 연쇄 보너스
  - 디버그 메뉴 (게임 파라미터 조정)

## 폴더 구조
```
GravityBlaster/
├── index.html          # 메인 HTML 파일
├── README.md           # 프로젝트 설명
├── CLAUDE.md           # 범용 개발 규칙
├── CLAUDE_PROJECT.md   # 프로젝트별 규칙 (이 문서)
├── css/
│   └── style.css       # 전체 스타일시트
└── js/
    ├── game.js         # 메인 게임 로직 및 초기화
    ├── board.js        # 보드 상태 관리
    ├── block.js        # 블록 클래스 정의
    ├── gravity.js      # 중력 시스템
    ├── animator.js     # 애니메이션 관리
    ├── render.js       # 캔버스 렌더링
    ├── input.js        # 입력 처리 (키보드/터치)
    ├── ui.js           # UI 관리 (메뉴, 팝업)
    └── utils.js        # 유틸리티 함수
```

## 주요 파일 설명

### 핵심 파일
- **game.js**: 게임 전체 흐름 제어, 초기화, 게임 루프
- **board.js**: 8x8 보드 상태 관리, 매칭 로직, 블록 제거/생성
- **gravity.js**: 중력 시스템, 블록 이동 로직
- **animator.js**: 블록 애니메이션 관리 (이동/제거/생성)

### 렌더링/UI
- **render.js**: HTML5 Canvas 렌더링
- **ui.js**: 메뉴, 팝업, 디버그 메뉴 관리
- **input.js**: 키보드(WASD/방향키) 및 터치 입력 처리

### 유틸리티
- **block.js**: Block 클래스 정의
- **utils.js**: 공통 유틸리티 함수

## 코드 구조 및 아키텍처 규칙

### 모듈 분리 원칙
- 각 JS 파일은 특정 역할에 집중
- 전역 객체를 통한 모듈 간 통신
- Canvas 렌더링과 게임 로직 분리

### 성능 최적화
- Canvas 더블 버퍼링 사용 금지 (단일 캔버스)
- DOM 쿼리 결과 캐싱 필수
- requestAnimationFrame 활용
- 애니메이션 완료 후 상태 업데이트

### 디버그 시스템
- 디버그 메뉴를 통한 파라미터 실시간 조정
- 애니메이션 속도, 보드 크기, 블록 생성 개수 등 설정 가능

## 현재 TODO
- [ ] 추후 작성 예정

## 개발 참고사항

### 게임 로직
- 블록은 중력 적용 시 개별 셀로 분해되어 이동
- 매칭은 같은 색 블록 3개 이상 인접(상하좌우)
- 4개 이상 매칭 시 보너스 점수
- 연쇄 매칭 시 추가 점수

### 애니메이션
- 제거 → 이동 → 생성 순서로 진행
- 각 애니메이션은 완료 후 다음 단계 실행
- 디버그 메뉴에서 각 애니메이션 속도 조정 가능

### 입력 처리
- WASD / 방향키: 중력 방향 변경
- 터치/스와이프: 모바일 지원
- ESC: 일시정지 (구현 여부 확인 필요)
